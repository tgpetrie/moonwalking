# Claude Instructions

Follow the shared workflow in `AGENTS.md` for all work in this repository. Before editing, also read `docs/ai/AI_INDEX.md`, `HANDOFF.md`, and `MW_BACKLOG.md`.

Use the active Moonwalking Git checkout connected to the project's authoritative remote; its local path may differ between computers and does not need to be inside OneDrive. Confirm it with `git remote -v`, `git status --short --branch`, and `git worktree list`. Do not use `DEVv\recovered\Moonwalking` for development or history repair. Repository files and Git history take precedence over remembered session context when they differ.
