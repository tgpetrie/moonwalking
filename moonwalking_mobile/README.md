
# Moonwalking Monorepo — Mobile + Worker + Sentiment + Learn

This repo scaffolds a robust mobile app (Expo React Native) backed by a Cloudflare Worker API.
Includes:
- `/data`, `/signals/pumpdump`, `/watchlist/*`, `/devices/register`
- `/sentiment` endpoint with connector stubs (Discord/Reddit/Telegram)
- "Learn" tab with Markdown lessons

## Quick Start
See README sections in each subfolder.
