# Legacy compatibility wrapper for sentiment-only setup.
# requirements.txt now carries the canonical shared pins; keep sentiment-specific
# packages here so this file can be installed alongside the other requirement files.
-r requirements.txt
fastapi==0.104.1
uvicorn[standard]==0.24.0
httpx==0.25.2
