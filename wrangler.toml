name = "moonwalking"
compatibility_date = "2024-08-13"

# Cloudflare Pages: where the static build is output
pages_build_output_dir = "frontend/dist"

# Note: Cloudflare Pages will run your build command in CI.
# Set the build command in the Pages project to: "cd frontend && npm ci && npm run build"