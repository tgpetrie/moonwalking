name: CI

on:
  push:
    branches: [ main, master, runninginmypepsteps ]
  pull_request:
    branches: [ main, master ]

jobs:
  lint-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('backend/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install backend deps
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run pytest (backend)
        working-directory: backend
        env:
          PYTHONWARNINGS: always::DeprecationWarning
        run: |
          pytest -q

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install frontend deps
        if: hashFiles('frontend/package.json') != ''
        working-directory: frontend
        run: |
          npm ci

      - name: Lint frontend
        if: hashFiles('frontend/package.json') != ''
        working-directory: frontend
        run: |
          npm run lint

      - name: Build frontend
        if: hashFiles('frontend/package.json') != ''
        working-directory: frontend
        run: |
          npm run build

      - name: Run frontend tests
        if: hashFiles('frontend/package.json') != ''
        working-directory: frontend
        run: |
          npm test --silent

# Optional: Cloudflare Pages/Workers deploy
# To enable, set secrets: CF_API_TOKEN and (if using wrangler) WRANGLER_API_TOKEN
# and uncomment the job below. Adjust publication steps depending on Pages vs Worker.
#
#  deploy-to-cloudflare:
#    needs: lint-test
#    runs-on: ubuntu-latest
#    steps:
#      - uses: actions/checkout@v4
#      - name: Upload to Cloudflare Pages
#        if: false # set to true and configure the following when ready
#        run: |
#          echo "Add deploy commands here (wrangler pages publish or wrangler deploy)"
#        env:
#          CF_API_TOKEN: ${{ secrets.CF_API_TOKEN }}
#
#      - name: Deploy Worker (wrangler)
#        if: false
#        run: |
#          npx wrangler pages publish frontend/dist --project-name=my-project
#        env:
#          WRANGLER_API_TOKEN: ${{ secrets.WRANGLER_API_TOKEN }}
