[pytest]
# Basic pytest config; tests may rely on this file being present in CI.
# We intentionally don't set environment variables here; instead a conftest
# ensures DISABLE_TALISMAN is set during pytest runs.
minversion = 6.0
addopts = -q
python_files = tests.py test_*.py *_tests.py
