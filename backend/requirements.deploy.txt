# Minimal runtime used by the always-on production container. The larger
# requirements.txt also contains offline/experimental ML tooling that is not
# imported by the live Flask board or the lightweight sentiment API.
flask==3.1.1
flask-cors==6.0.1
flask-talisman==1.1.0
requests==2.32.2
cdp-sdk>=1.28,<2
websocket-client==1.9.0
gunicorn==21.2.0
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.9,<3
python-dotenv==1.0.1
psutil==6.1.1
