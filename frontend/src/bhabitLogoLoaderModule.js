// Disabled BHABIT logo loader module - no-op to prevent automatic logo injection during dev
// If you need to re-enable the logo loader, restore the original implementation above.
export default function bhabitLogoLoaderModuleNoop() {
  // intentionally empty
}

