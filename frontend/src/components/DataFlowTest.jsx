import React from 'react';

// Minimal DataFlowTest placeholder to satisfy import during build.
export default function DataFlowTest() {
  // No-op during restore/build. Can be expanded later to show websocket/debug info.
  return null;
}
